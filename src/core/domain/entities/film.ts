export class Film {
  id: number
  title: string
  director: string

  constructor(id: number, title: string, director: string) {
    this.id = id
    this.title = title
    this.director = director
  }
}
